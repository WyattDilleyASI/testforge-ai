{
  "name": "testforge-ai",
  "version": "1.2.0",
  "description": "AI-Powered Test Creation Tool — Full-stack deployment",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build:client": "cd client && npm install && npm run build",
    "setup": "npm install && cd client && npm install && npm run build",
    "postinstall": "echo '✓ Server dependencies installed. Run npm run build:client to build the frontend.'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^11.0.0",
    "bcryptjs": "^2.4.3",
    "express-session": "^1.17.3",
    "connect-sqlite3": "^0.9.15",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "dotenv": "^16.4.5"
  }
}
